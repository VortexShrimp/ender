#pragma once
#include "../../engine/platform/window.hpp"

namespace snake {
    class game {
    public:
    private:
        ender::unique_pointer<ender::game_window> m_window;
    };
}  // namespace snake
